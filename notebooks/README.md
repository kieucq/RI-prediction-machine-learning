# Notebooks

Notebook outputs have been stripped to keep the repository lightweight. Each notebook starts with a setup cell that adds `src/` to `sys.path` and defines `TRAINING_DATA_DIR`, `CASE_DATA_DIR`, `EXAMPLE_DATA_DIR`, `MODEL_DIR`, and `RESULTS_DIR`.
